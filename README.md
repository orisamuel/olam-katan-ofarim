# עולם קטן · הירוקה בעופרים 🌿

מיניסייט הומוריסטי (אתר אחות ל־[datiim-shluvim](https://github.com/orisamuel/datiim-shluvim)).

מכירים לך **זוג השבוע** — נועם ויעל שגרים בהירוקה בעופרים — ומנהלים איתך **שיחת בילוש** קצרה כדי למצוא את החוליה שמחברת ביניכם. למערכת "יש" תיק פרטים על הזוג, אבל היא **לא מגלה אותם** — רק שואלת, ורק אם קלעת חושפת את הקישור. והקישור לא בנאלי:

- שאלה **סגורה** וחשופה, בלי רמז: *"למדת במרכז הרב?"*
  - **כן** → חושפת קישור מפתיע דרך צד־שלישי: *"אז אתה מכיר את יענקל׳ה מהמכולת שממול — זה הדוד של נועם, והשדכן שלהם."* 🤯
  - **לא** → *"אין בעיה, אז איפה כן?"* (נפתחת לשאלה פתוחה) → ממשיכים לכיוון הבא, **בלי לגלות כלום**: *"עכשיו לגבי אשתך…"*
- הכיוונים: ישיבה → שירות לאומי של האישה → קרוב משפחה ביישוב → שבט בבני עקיבא. הראשון שקולע = החוליה. אם אף אחד לא קלע — נסגר בחיבור דרגה־שנייה ("עולם קטן, מכר אחד ביניכם").

## החלפת זוג שבועית
כל הזוג מוגדר באובייקט `COUPLE` בראש ה־`<script>` שב-[index.html](index.html). כדי לשים זוג חדש בכל שבוע — עורכים רק את האובייקט הזה:
- `names` / `bio` / `sub` — שם ותיאור הזוג.
- `avenues[]` — לכל כיוון: `probe` (השאלה הסגורה עם הפרט הידוע), `hit` + `revealLine` (תגובה אם קלעת), `openQ` + `openChips` + `openReact` (אם פספסת), ו-`weave` (ניסוח לחיבור דרגה־שנייה).

אין צורך לגעת בשאר הקוד.

## הרצה
דף סטטי בודד, HTML/CSS/JS וניל, ללא תלויות וללא שרת. פותחים את `index.html` או דרך GitHub Pages. עברית RTL, פונטים Rubik/Heebo. כל הנתונים נשארים בדפדפן.

---
אתר סאטירי — הזוג בדיוני. בהשראת חוק דתיים שלובים.
